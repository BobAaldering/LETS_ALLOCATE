#ifndef ALLOCATOR_BENCHMARK_PLOT_GENERATOR_H
#define ALLOCATOR_BENCHMARK_PLOT_GENERATOR_H

#include <fstream> // Include here all the 'fstream' facilities.
#include <tuple> // Include here all the 'tuple' facilities.
#include <ctime> // Include here all the 'ctime' facilities.
#include <iomanip> // Include here all the 'iomanip' facilities.
#include <sstream> // Include here all the 'sstream' facilities.

#include "benchmark_statistics.h" // Include here all the 'benchmark_statistics' facilities.

/// This is the class 'benchmark_plot_generator' for generating various plots for our allocator benchmarker.
class benchmark_plot_generator {
public:
    /// This is the constructor of the 'benchmark_plot_generator' class.
    /// \param statistics_of_benchmark A vector that contains all the values generated by the benchmarker function.
    explicit benchmark_plot_generator(std::vector<benchmark_statistics> statistics_of_benchmark);

    /// This method generates a MATLAB-plot with our data from the benchmark function.
    void generate_matlab_plot() const;

private:
    std::vector<benchmark_statistics> m_statistics; // This field contains a vector with all the statistics from the benchmarker.
};

#endif